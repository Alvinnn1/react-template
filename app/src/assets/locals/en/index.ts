import {Locals} from "@imf/typelocals";
import {errorLocals} from "@/assets/locals/en/ErrorLocals";
import {labelLocals} from "@/assets/locals/en/LabelLocals";
import {segmentLocals} from "@/assets/locals/en/SegmentLocals";

const EN: Locals = {
    error: errorLocals,
    label: labelLocals,
    segment: segmentLocals
}
export default EN