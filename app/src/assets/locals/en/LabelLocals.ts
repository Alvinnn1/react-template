import { Locals } from '@imf/typelocals'
import { LabelLocals } from '@/assets/locals/LabelLocals'

export const labelLocals: LabelLocals = {
  'login-toast': (name: string) => `Please sign in ${name}`,
  profile: 'Profile',
}
